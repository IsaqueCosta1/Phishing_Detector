[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "phishing-detector"
version = "0.1.0"
description = "Detector de phishing a partir de features de URL/página"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
